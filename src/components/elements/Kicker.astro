---
// Fundations
import Text from "./Text.astro"
const { class: className = "" } = Astro.props
const baseClasses = "uppercase"
const combinedClasses = `${baseClasses} ${className}`.trim()
---

<Text tag="p" variant="textSM" class={combinedClasses}>
  <slot />
</Text>
